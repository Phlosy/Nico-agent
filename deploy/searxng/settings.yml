use_default_settings: true

general:
  instance_name: Nico Local Search
  debug: false

search:
  safe_search: 1
  formats:
    - html
    - json

server:
  bind_address: 0.0.0.0
  port: 8080
  secret_key: nico-local-searxng-not-for-production
  limiter: false
  image_proxy: false
