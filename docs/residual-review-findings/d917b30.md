# Known residual review findings

Source review run: `/tmp/compound-engineering-1000/ce-code-review/20260721-071858-468382b3`

Reviewed implementation commit: `d917b30`

These findings were explicitly accepted during the autonomous shipping tail. They are
non-blocking follow-up work and were not dropped from the review record.

## P2 - Project tasks performs per-session requests

- Finding: `#15`
- Location: `backend/src/nico_agent/cli/project.py:299`
- Status: accepted residual
- Reason: resolving this requires a new Project-scoped paginated API contract. The current CLI
  is correct, follows every timeline page, and bounds each request; adding a public aggregation
  endpoint is outside this plan's accepted contract.
- Follow-up: add a Project-scoped paginated task/timeline summary endpoint, then update
  `ProjectCli.tasks` to use one request while preserving its sorting and output contract.

## P2 - Move collaboration ORM models into projects package

- Finding: `#17`
- Location: `backend/src/nico_agent/domain/models.py:254`
- Status: accepted residual
- Reason: this is a structural maintainability preference rather than a behavior defect. Moving
  mapped classes after the migrations and APIs are stable creates avoidable import-cycle and
  metadata-registration risk during this delivery.
- Follow-up: evaluate moving `ProjectMember`, `ProjectSession`, `ProjectSupervisionCycle`, and
  `RunIntervention` into `nico_agent.projects.models` as a dedicated refactor with migration and
  integration verification.
